//WAP if persom is elligible for voting or not.//
#include<stdio.h>
int main()
{
   int age;
   printf("enter the age");
   scanf("%d",&age);
   if(age>=18)
   {
     printf("person is elligible for voting");
   }
   else
   {
   printf("person is not elligible for voting\n");
   }
   return 0;
 } 
